version: '2'

options:
  verbose: false

syncs:
  unison-sync:
    src: ${SOURCE_DIR}
    sync_strategy: ${DOCKER_SYNC_STRATEGY}
    sync_userid: 1000
    sync_excludes: [
      '.gitignore',
      '.git/',
      '.DS_Store',
    ]
